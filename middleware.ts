// Middleware removed - authentication is now handled at page level
// Protected pages use validateAuthToken() to ensure token validity
// See: /src/app/membership/members-area/page.tsx and [...uri]/page.tsx
